<h1>商品詳細</h1>

<div>
  <%= item_image_tag @item.item_image, size: "300x200" %>
</div>

<table>
  <tr>
    <td>商品名</td>
    <td><%= @item.name %></td>
    
    <td>商品説明</td>
    <td><%= @item.name_explanation %></td>
    
    <td>ジャンル</td>
    <td><%= @genre.name %></td>
    
    <td>税込価格(税抜価格)</td>
    <td><%= @item.with_tax_price %>(<%= @item.price %>)</td>
    
    <td>販売ステータス</td>
    <td><% if item.is_sales_status %>
          <strong>販売中</strong>
          <% else %>
          <strong>販売停止中</strong>
          <% end %>
    </td>
  </tr>
</table>

<div>
  <%= link_to "編集する", edit_admin_item_path 
</div>